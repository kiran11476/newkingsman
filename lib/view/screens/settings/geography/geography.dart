import 'package:flutter/material.dart';

class ScreenGeo extends StatelessWidget {
  const ScreenGeo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
